# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
android.nonTransitiveRClass=true

# JVM tuning for Kotlin/Compose; keep memory reasonable for CI
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8 -XX:+UseParallelGC

# Single-use daemon guidance to avoid Docker daemon conflicts in CI
org.gradle.daemon=false
org.gradle.workers.max=1
org.gradle.parallel=false

# Prefer stable and cache-friendly builds
org.gradle.configuration-cache=false
org.gradle.caching=true

# Avoid incubating features that can cause warnings in CI
org.gradle.configureondemand=false
