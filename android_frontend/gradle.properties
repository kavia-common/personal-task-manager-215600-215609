# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
android.nonTransitiveRClass=true

# Tweak Gradle/JVM to avoid daemon issues in CI and provide enough memory for Kotlin/Compose
org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8 -XX:+UseParallelGC

# Avoid too many workers/daemons that can exceed CI limits
org.gradle.daemon=true
org.gradle.workers.max=2
org.gradle.parallel=false

# Enable configuration cache for faster, stable builds
org.gradle.configuration-cache=true
org.gradle.caching=true
